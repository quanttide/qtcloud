# AGENTS.md

本仓库是量潮云产品仓库：门户（`src/site`）、CoWork 工作台（`src/studio`）、服务端骨架（`src/provider`）、原型（`examples/`）。通用协作约定与 SKILL 索引见 `.agents/README.md` 与主仓库 `AGENTS.md`。

## 交互设计评判原则

适用于本仓库一切带界面的交付物（site、studio、examples），评审与实现都按此执行。

### 先立问题，再做屏幕

1. 动手前写下「用户打开这个屏幕时心里的问题」，每个界面元素必须能追溯到它
2. 第一屏回答用户的第一个问题（通常是「现在该我干什么」），而不是展示系统结构

### 评判断据（违反即减分）

3. **工作物可见**：用户对着被加工的对象干活。只有任务清单而没有工作物本身（文档、数据、草案）的是任务管理器，不是工作空间
4. **状态即邀请**：什么在等我、什么就绪了，通过视觉优先级直接呈现，不靠逐个读标签；就绪要有调度语义（上游完成判定），不能只画依赖
5. **主行动唯一**：全屏只有一个主按钮层级，其余退后、折叠
6. **动作有回应**：每个操作都有可感知的反馈（toast、高亮、完成时刻）；没有回应的点击等于界面失信
7. **不演戏**：模拟的能力诚实标注；假延迟、假动画冒充智能比不做更糟
8. **验收有对象**：通过 / 驳回时必须展示被判定的工作物本体，不允许只看一行摘要
9. **中文微排版**：正文 ≥ 14px，辅助文字 ≥ 12px；更小的中文文字一律视为缺陷

### 交付纪律

10. **脑内渲染走查**：提交前以用户角色走查整屏——第一眼看哪、第一步做什么、动作之后世界如何回应。看 spec 与代码就要能预感手感，不等「跑起来再说」
11. **敢于自评分**：交付时按评分标准给出分数与理由；不敢给分等于没有内部标准
12. **分数在走查之后**：自评分必须以第 10 条走查为前提，走查发现的缺陷未清零不得交付——先给分后走查，分数就是拍脑袋

| 分值 | 标准 |
|:----:|:-----|
| 10 | 敢给真实用户用 |
| 7 | 团队内部可日常使用 |
| 5 | 完整演示概念且不丢分 |
| 3 | 能跑但用起来难受 |
| 1 | 概念错误 |

## 案例存照

`examples/studio`（2026-09）：v1 是数据结构的投影——六卡平铺、状态靠读标签、无调度语义；v2 过程正确（先立问题）但自评 3 分——空间里没有工作物、派活演戏、验收没有对象。教训全文见 `examples/studio/README.md`。

新界面开工前先读本文件与该案例。
